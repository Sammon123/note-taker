// GET '/api/notes' responds with all the notes from the database with getNotes

// POST '/api/notes' update the current note, with addNote. If not send a error in json with an error of 500

// DELETE '/api/notes' deletes the note with an id equal to (req.params.id) with removeNotes

// export router function