// generate a unique id with uuid

// if notes isn't an array or can't be turned into one, send an empty array

// add a unique id to the note using uuid

// get all the notes, add the new note, write all the updated notes, then return 'newNote'

// get all the notes, remove note with given id, write the filtered notes

// export function